[phases.setup]
nixPkgs = ['python3', 'nodejs_20', 'postgresql_16.dev', 'gcc']

[phases.install]
cmds = [
  'python -m venv --copies /opt/venv',
  '. /opt/venv/bin/activate && pip install -r requirements.txt'
]

[phases.build]
cmds = ['./build.sh']

[start]
cmd = 'uvicorn main:app --host 0.0.0.0 --port $PORT'
