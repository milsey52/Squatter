[phases.setup]
nixPkgs = ["python39", "nodejs-18_x"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd web-client && npm install"
]

[phases.build]
cmds = ["cd web-client && npm run build"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
